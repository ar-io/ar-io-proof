# Pinned ar-io-agent commit for the Go leg of the cross-kernel agreement gate.
# The Go kernel (pkg/proof) is materialized at this commit by run.sh — a
# detached git worktree from a sibling ../ar-io-agent checkout, same pattern
# as the proof-checker's wasm/PIN. Re-pin (and re-baseline cases.json) is a
# deliberate act, never a floating HEAD.
#
# NOTE: currently pins the kernel/full-family branch tip (PR #20), which
# carries the external-commitment + §2 + #13 behavior this gate asserts.
# Re-pin to the merged-main commit once #20 lands.
agent_commit=ca18a7bf32b72898b16fe6fe0c90b529b84201af
go_version=go1.25.0
