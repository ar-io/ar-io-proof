# Pinned ar-io-agent commit for the Go leg of the cross-kernel agreement gate.
# The MIT-carved-out pkg/proof is VENDORED at this commit under
# vendor-agent/pkg/proof/ (see vendor-agent/pkg/proof/VENDORING.md). The Go leg
# builds the vendored source — no private-repo access, runs on public/fork PRs.
# Re-pin = re-sync the vendored files + bump this commit (a deliberate act,
# never a floating HEAD).
agent_commit=ebe2ecd60395328ff67e28f0ec4fd9e78f42da06
go_version=go1.25.0
